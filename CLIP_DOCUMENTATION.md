# Clip — Документация

## Обзор

**Clip** — мобильное приложение для Android (и iOS) на базе Expo + React Native. Личная база знаний из идей и мыслей с механикой ежедневного возврата. Всё хранится локально на устройстве — никакого бэкенда.

---

## Технический стек

| Технология | Версия | Назначение |
|---|---|---|
| Expo | ~52 | Платформа для React Native |
| React Native | — | UI-фреймворк |
| Expo Router | ~6 | Файловая маршрутизация |
| AsyncStorage | 2.2.0 | Локальное хранилище |
| expo-notifications | ~0.32.16 | Push-уведомления |
| @expo/vector-icons | — | Иконки (Feather) |
| expo-haptics | — | Тактильная обратная связь |
| react-native-reanimated | — | Анимации |

---

## Структура проекта

```
artifacts/clip-app/
├── app/
│   ├── _layout.tsx              # Корневой layout, провайдеры, онбординг-редирект
│   ├── onboarding.tsx           # Экран онбординга (3 шага)
│   ├── add.tsx                  # Экран добавления цитаты (modal)
│   ├── settings.tsx             # Экран настроек (тема, время уведомлений)
│   ├── clip/
│   │   └── [id].tsx             # Детальный просмотр цитаты
│   └── (tabs)/
│       ├── _layout.tsx          # Tab bar (NativeTabs / Classic)
│       ├── index.tsx            # Главный экран (Сегодня)
│       └── archive.tsx          # Архив
├── src/
│   ├── storage/
│   │   └── clips.ts             # AsyncStorage CRUD + типы Settings, ThemeMode
│   ├── notifications/
│   │   └── digest.ts            # Планирование push-уведомлений
│   ├── context/
│   │   ├── ClipsContext.tsx     # Глобальный стейт цитат (React Context)
│   │   └── ThemeContext.tsx     # Глобальный стейт темы (dark/light/system)
│   ├── utils/
│   │   └── pluralize.ts         # Русские склонения ("идея/идеи/идей")
│   └── components/
│       ├── ClipCard.tsx         # Карточка цитаты
│       └── TagPicker.tsx        # Компонент выбора тегов
├── constants/
│   └── colors.ts                # Дизайн-токены (палитры dark и light)
├── hooks/
│   └── useColors.ts             # Хук, возвращающий активную палитру через ThemeContext
└── app.json                     # Конфигурация Expo
```

---

## Экраны

### OnboardingScreen (`app/onboarding.tsx`)

Показывается при первом запуске. Сохраняет флаг `onboardingDone = true` в настройках через AsyncStorage.

**Шаг 1 — Первая идея**
- Заголовок: «Ты читаешь много. Помнишь мало. Clip это исправит.»
- Подзаголовок: «Начни с первой идеи — той, которую хочется перечитывать.»
- Открытое поле ввода («Введи идею или мысль...»)
- Кнопка "Сохранить и продолжить" → сохраняет идею и переходит дальше
- Кнопка "Позже" → пропускает шаг без сохранения

**Шаг 2 — Share Intent**
- Заголовок: «Добавляй из любого приложения»
- Подзаголовок: «Увидел важную мысль — поделись с Clip. Три секунды, и она в архиве навсегда.»
- Визуальная инструкция из 3 шагов: выдели → поделись → Clip
- Кнопка "Понял, продолжить"

**Шаг 3 — Время уведомлений**
- Заголовок: «Когда присылать лучшее из того что ты читал?»
- Три кнопки: "Утром (8:00)", "Вечером (20:00)", "Не сейчас"
- При выборе времени: запрашивает разрешение, планирует ежедневный push
- Переходит на главный экран

---

### HomeScreen (`app/(tabs)/index.tsx`)

Главный экран с двумя зонами.

**Верхняя зона — дневные карточки**
- Дата сегодня + заголовок "Сегодняшние открытия"
- Под заголовком — счётчик "**N** идей в архиве" (склоняется через `clipsCount()`, акцентное число янтарным)
- Тихая строка-streak «🔥 N дней подряд» (число янтарным, текст приглушённым). Показывается только когда `streak.count ≥ 1` и `lastDate` это сегодня или вчера; иначе скрыта
- Иконка-шестерёнка справа → переход на `/settings`
- 3 случайные карточки из архива (фиксируются на день через `getDailyCards()`)
- При пустом архиве — empty state с кнопкой "Сохранить первую идею"

**Нижняя зона**
- Кнопка "✦ Удиви меня" → показывает случайную карточку в анимированном модальном окне
- Быстрое поле ввода для добавления идеи прямо с главного экрана («Быстро добавить идею…»)
- FAB "+" в правом нижнем углу → открывает AddClipScreen

---

### ArchiveScreen (`app/(tabs)/archive.tsx`)

**Счётчик** — в правом верхнем углу показывает «47 идей» с правильным склонением. При активном поиске или фильтре по тегу формат «12 из 47 идей».

**Поиск** — строка поиска фильтрует в реальном времени по тексту и тегам.

**Фильтр по тегам** — горизонтальный скролл: "Все" + по одному тегу на каждый существующий.

**Список** — FlatList всех цитат, новые сверху. Компонент `ClipCard` в компактном режиме.

**Long press** → Alert с подтверждением удаления.

---

### AddClipScreen (`app/add.tsx`)

Открывается как modal снизу.

- Поле текста идеи (обязательное, multiline)
- Компонент TagPicker для выбора/создания тегов
- Если источник не "manual" (приходит из Share Intent) — отображается бейдж источника
- При достижении лимита 100 идей — показывает баннер и блокирует сохранение
- Кнопки: "✕" (закрыть) слева, "Сохранить" (янтарная) справа

---

### ClipDetailScreen (`app/clip/[id].tsx`)

Открывается по тапу на карточку.

- Полный текст идеи крупным шрифтом с янтарной полосой-акцентом
- Кнопка "Редактировать" рядом с текстом → inline-режим: `TextInput` с янтарной рамкой и автофокусом, кнопка "Сохранить" снизу. Защита от пустого текста, пропуск записи, если текст не изменился
- Метаданные: источник, дата
- Редактирование тегов (inline через TagPicker)
- Кнопка "Поделиться" → нативный Share
- Кнопка "Удалить" (с подтверждением Alert)

---

### SettingsScreen (`app/settings.tsx`)

Открывается по нажатию на шестерёнку в шапке главного экрана.

**Раздел "Уведомления"**
- Сетка из 11 пресетов (07:00, 08:00, 09:00, 10:00, 12:00, 14:00, 17:00, 19:00, 20:00, 21:00, 22:00). Активный — янтарный
- Кнопка "Не присылать" — отменяет расписание через `cancelDailyDigest()`
- Под сеткой — статус: «Активно: каждый день в 20:00» / «Уведомления отключены» / «Сохраняю…»
- При выборе нового часа: запрашивается разрешение → старое расписание отменяется → планируется новое (через `scheduleDailyDigest`)

**Раздел "Внешний вид"**
- Три карточки: Тёмная (moon), Светлая (sun), Системная (smartphone). Активная подсвечена янтарной рамкой и фоном `accentSubtle`
- Выбор сохраняется в `settings.themeMode` и сразу применяется через `ThemeContext` — все экраны автоматически перекрашиваются

---

## Компоненты

### ClipCard (`src/components/ClipCard.tsx`)

```
┌─────────────────────────────┐
│▌ Текст цитаты (до 4 строк   │
│  в компактном режиме)       │
│                             │
│  Telegram · 9 апр 2026     │
│  #психология #продукт       │
└─────────────────────────────┘
```

- Левая янтарная полоса-акцент
- Тёмный фон (#181818)
- Пропсы: `clip`, `onPress`, `onLongPress`, `compact`

### TagPicker (`src/components/TagPicker.tsx`)

- Горизонтальный скролл существующих тегов
- Выбранные теги подсвечиваются янтарным
- Поле для создания нового тега + кнопка добавления

---

## Storage Layer (`src/storage/clips.ts`)

Все функции асинхронные, работают с AsyncStorage.

| Функция | Возвращает | Описание |
|---|---|---|
| `getAllClips()` | `Clip[]` | Все цитаты, новые сверху |
| `saveClip(clip)` | `Clip` | Сохраняет новую цитату, обновляет streak |
| `deleteClip(id)` | `void` | Удаляет по ID |
| `updateClip(id, changes)` | `void` | Обновляет поля цитаты |
| `getDailyCards()` | `Clip[]` | 3 случайных цитаты (фиксируются на день) |
| `getRandomClip()` | `Clip \| null` | Одна случайная цитата |
| `getAllTags()` | `string[]` | Все уникальные теги |
| `getStreak()` | `Streak` | Текущий streak |
| `getSettings()` | `Settings` | Настройки приложения |
| `saveSettings(s)` | `void` | Сохраняет настройки |

**Константа:** `FREE_LIMIT = 100`

**Структура Clip:**
```typescript
{
  id: string;          // Auto-generated
  text: string;        // Текст цитаты
  imageUri: null;      // Зарезервировано для скриншотов
  source: string;      // 'manual' | название приложения
  tags: string[];      // Теги
  createdAt: string;   // ISO строка
}
```

**Структура Settings:**
```typescript
{
  notificationHour: number | null;          // Час пуша (0–23) или null = выключено
  onboardingDone: boolean;                  // Прошёл ли онбординг
  themeMode: "dark" | "light" | "system";   // Выбор темы
}
```

При чтении старых записей недостающие поля автоматически дополняются из `DEFAULT_SETTINGS` (миграция через `{ ...DEFAULT_SETTINGS, ...JSON.parse(raw) }`).

**AsyncStorage ключи:**
- `@clip:clips` — массив всех цитат
- `@clip:streak` — данные streak
- `@clip:settings` — настройки (notificationHour, onboardingDone, themeMode)
- `@clip:daily_cards` — ID карточек на сегодня
- `@clip:daily_date` — дата последней генерации дневных карточек

---

## Push-уведомления (`src/notifications/digest.ts`)

| Функция | Описание |
|---|---|
| `requestNotificationPermission()` | Запрашивает разрешение системы |
| `scheduleDailyDigest(hour)` | Планирует ежедневный push в указанный час |
| `cancelDailyDigest()` | Отменяет все запланированные уведомления |

**Формат уведомления:**
- Заголовок: «Идея дня из твоей базы»
- Текст: первые 60 символов случайной идеи + "..."

На web push-уведомления не поддерживаются (автоматически отключены через `Platform.OS === 'web'`).

---

## Глобальный стейт

В `app/_layout.tsx` приложение оборачивается в два провайдера: `ThemeProvider` снаружи (чтобы `useColors` работал везде), `ClipsProvider` внутри.

### `ClipsContext` (`src/context/ClipsContext.tsx`)

| Значение | Тип | Описание |
|---|---|---|
| `clips` | `Clip[]` | Все цитаты |
| `dailyCards` | `Clip[]` | Карточки на сегодня |
| `allTags` | `string[]` | Все теги |
| `loading` | `boolean` | Состояние загрузки |
| `reachedLimit` | `boolean` | Достигнут ли лимит |
| `addClip()` | функция | Добавить цитату |
| `removeClip()` | функция | Удалить цитату |
| `editClipTags()` | функция | Изменить теги |
| `editClipText()` | функция | Изменить текст цитаты |
| `getRandomOne()` | функция | Случайная цитата |
| `refresh()` | функция | Перезагрузить данные |

### `ThemeContext` (`src/context/ThemeContext.tsx`)

| Значение | Тип | Описание |
|---|---|---|
| `mode` | `"dark" \| "light" \| "system"` | Выбор пользователя из настроек |
| `scheme` | `"dark" \| "light"` | Эффективная схема (для `mode: "system"` берётся из `Appearance`) |
| `setMode(m)` | функция | Сохраняет выбор и сразу применяет |

При маунте читает `themeMode` из `getSettings()`. Подписан на `Appearance.addChangeListener` — если режим `system`, тема меняется вместе с настройками устройства.

---

## Дизайн-система

Поддерживаются обе палитры: **тёмная** (по умолчанию) и **светлая**. Активная палитра выбирается через `ThemeContext` и возвращается хуком `useColors()`. Все токены в `constants/colors.ts`.

### Тёмная палитра (Moleskine, тёплая)

| Токен | Значение | Описание |
|---|---|---|
| `background` | `#0D0D0D` | Фон приложения |
| `card` / `bgCard` | `#181818` | Фон карточек |
| `bgInput` | `#1E1E1E` | Фон полей ввода |
| `foreground` | `#F0EDE6` | Основной текст (тёплый белый) |
| `textSecondary` | `#8A8580` | Второстепенный текст |
| `textMuted` | `#4A4744` | Приглушённый текст |
| `primary` / `accent` | `#F5C842` | Янтарный акцент |
| `accentDim` | `#8A6F1E` | Приглушённый янтарь |
| `accentSubtle` | `#2A2210` | Фон акцентных элементов |
| `border` | `#2A2A2A` | Границы |

### Светлая палитра (бумага)

| Токен | Значение | Описание |
|---|---|---|
| `background` | `#FAF8F2` | Тёплый off-white |
| `card` / `bgCard` | `#FFFFFF` | Чисто-белые карточки |
| `bgInput` | `#F5F2EC` | Фон полей ввода |
| `foreground` | `#1A1815` | Тёмный тёплый текст |
| `textSecondary` | `#67645F` | Второстепенный текст |
| `textMuted` | `#A8A39C` | Приглушённый текст |
| `primary` / `accent` | `#B8901C` | Янтарный (затемнён для контраста на светлом) |
| `accentDim` | `#8A6F1E` | Тёмный янтарь для текста на цветных бейджах |
| `accentSubtle` | `#FBF4DA` | Светлый янтарный для подсветки |
| `border` | `#E5E2DC` | Бежевые границы |

**Шрифт:** Inter (400 / 500 / 600 / 700)

**Радиус:** 8px (базовый), 10–16px для карточек и модалей

---

## Навигация

Expo Router с файловой маршрутизацией.

```
/ (tabs)
  ├── index     → Главный экран (Сегодня)
  └── archive   → Архив

/onboarding    → Онбординг (redirect при первом запуске)
/add           → Добавление цитаты (modal)
/clip/[id]     → Детальный просмотр
/settings      → Настройки (тема, время уведомлений)
```

Tab bar поддерживает NativeTabs с Liquid Glass на iOS 26+ и классические вкладки с BlurView на остальных платформах.

---

## Проверка работы

### Ручное тестирование

1. **Онбординг:** При первом запуске должен показываться онбординг. После прохождения — редирект на главный экран. Повторный запуск — сразу главный экран.

2. **Добавление идеи:**
   - Нажать "+" (FAB) или ввести текст в быстрое поле внизу
   - Ввести текст → добавить теги → нажать "Сохранить"
   - Идея должна появиться в архиве

3. **Главный экран:**
   - После добавления 1+ идей должны появиться дневные карточки
   - Кнопка "Удиви меня" → открывает случайную идею

4. **База знаний (архив):**
   - Заголовок экрана — «Твоя база знаний»
   - Поиск по тексту работает в реальном времени
   - Фильтр по тегам фильтрует список
   - Long press на карточке → подтверждение удаления

5. **Детальный просмотр:**
   - Тап на карточку → полный текст
   - Кнопка "Редактировать" → TagPicker inline
   - Кнопка "Поделиться" → нативный Share
   - Кнопка "Удалить" → подтверждение → возврат в архив

6. **Лимит:** При 100+ идеях добавление блокируется с баннером.

7. **Уведомления:** На реальном устройстве (Android/iOS) — выбор времени в онбординге запрашивает разрешение и планирует ежедневный push.

---

## Share Intent

Приложение поддерживает захват текста из других приложений через нативный механизм Share.

### Как это работает

Пользователь выделяет текст в Telegram, браузере или любом другом приложении, нажимает «Поделиться» и выбирает Clip — приложение открывается с предзаполненным текстом на экране добавления.

### Реализация

| Файл | Роль |
|---|---|
| `package.json` | `expo-share-intent@5.1.1` — совместима с Expo SDK 54 |
| `app.json` | Плагин `expo-share-intent` с фильтрами для текста и URL |
| `app/_layout.tsx` | `ShareIntentProvider` оборачивает дерево навигации; компонент `ShareIntentHandler` следит за входящими интентами |

### Конфигурация плагина (`app.json`)

```json
[
  "expo-share-intent",
  {
    "iosActivationRules": {
      "NSExtensionActivationSupportsText": true,
      "NSExtensionActivationSupportsWebURLWithMaxCount": 1
    },
    "androidIntentFilters": ["text/*"]
  }
]
```

- **iOS** — поддерживает текст (`NSExtensionActivationSupportsText`) и веб-URL (лимит 1)
- **Android** — фильтр `text/*` захватывает любой текстовый контент

### Логика `ShareIntentHandler`

```tsx
// Внутри провайдера, следит за hasShareIntent
const { hasShareIntent, shareIntent, resetShareIntent } = useShareIntentContext();

useEffect(() => {
  if (!hasShareIntent) return;
  const text = shareIntent?.text ?? shareIntent?.webUrl ?? null;
  if (!text) return;

  router.push({
    pathname: "/add",
    params: {
      sharedText: text,
      source: shareIntent?.meta?.title ?? "share",
    },
  });
  resetShareIntent(); // очищает интент после навигации
}, [hasShareIntent, shareIntent]);
```

- `shareIntent.text` — выделенный текст
- `shareIntent.webUrl` — URL если поделились ссылкой
- `shareIntent.meta?.title` — название приложения-источника (например, «Telegram»)
- После навигации `resetShareIntent()` сбрасывает состояние

### Ограничения

- **Expo Go / Web**: Share Intent отключается автоматически через `disabled: Platform.OS === 'web'`. Функция доступна только в нативной production-сборке.
- Для тестирования на симуляторе нужен `expo prebuild` + `expo run:android` / `expo run:ios`.
- В production-сборке для iOS используется Replit Expo Launch (App Store).

---

## Ограничения MVP

- Share Intent требует нативной сборки — в Expo Go и web-preview недоступен (отключён автоматически)
- Push-уведомления работают только на реальном устройстве (не в Expo Go на web)
- Лимит 100 идей в бесплатной версии
- Скриншоты (`imageUri`) зарезервированы, но не реализованы в MVP
